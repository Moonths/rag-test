"""
===========================================================================
📌 utils/__init__.py — 工具包初始化文件
===========================================================================

🔰 新手导读：
__init__.py 是 Python 包的初始化文件，当你 import 这个包时会自动执行。
这里做了两件事：
  1. 设置全局时区
  2. 创建全局日志实例（logger），供整个项目使用

💡 为什么需要全局 logger？
  - 日志是调试和监控的重要工具
  - 全局创建一次，其他模块直接 `from utils import logger` 即可使用
  - 统一日志格式，方便排查问题
===========================================================================
"""

# 设置全局时区（确保不同环境下时间一致）
from time import tzset
tzset()

# 创建全局日志实例
# 其他模块可以直接: from utils import logger
# 然后使用: logger.info("消息"), logger.error("错误") 等
from .get_logger import get_logger
logger = get_logger()
